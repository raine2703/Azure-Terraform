Project for Modules

Creating Networkig module and Storage account module (new Folder). 

Module is like function. Can be reused several times by passing variables to them.

Its also possible to retrieve values back from modules with outputs. As example using Networking module outputs to create VM.

